## 修复 Word 公式乱码的方法

### 问题原因
LaTeX 公式（`$$...$$`）直接作为纯文本插入 Word，或者用 PNG 图片渲染导致字号不匹配、显示为乱码。

### 解决方案

**核心思路**：不用 LaTeX 渲染/图片，而是写一个 LaTeX → Unicode 文本的解析器，把公式转换成普通文本（Times New Roman 字体 + Unicode 上下标/希腊字母）。

### 替换文件

把原有的 `md_to_docx.py` 中处理公式的部分替换为以下代码。

**步骤 1** — 在文件顶部添加以下函数（放在 import 后面）：

```python
# ─── LaTeX 块级公式 → Unicode 排版 ───
GREEK = {'alpha':'α','beta':'β','sigma':'σ','mu':'μ','delta':'δ','theta':'θ','pi':'π'}
SYMS = {'cdot':'·','times':'×','cdots':'…','ldots':'…',
        'leq':'≤','geq':'≥','le':'≤','ge':'≥','to':'→',
        'infty':'∞','partial':'∂','sum':'Σ','prod':'Π','int':'∫'}
TEXT_TAGS = ('mathrm', 'text', 'textbf', 'mathit', 'mathbf', 'operatorname')

SUB_MAP = {'0':'₀','1':'₁','2':'₂','3':'₃','4':'₄','5':'₅',
       '6':'₆','7':'₇','8':'₈','9':'₉',
       't':'ₜ','s':'ₛ','p':'ₚ','x':'ₓ','n':'ₙ','m':'ₘ',
       'k':'ₖ','j':'ⱼ','i':'ᵢ'}
SUP_MAP = {'0':'⁰','1':'¹','2':'²','3':'³','4':'⁴','5':'⁵',
       '6':'⁶','7':'⁷','8':'⁸','9':'⁹','T':'ᵀ','N':'ᴺ'}


def _find_matching_brace(s, start):
    """从 start 位置开始找到匹配的右花括号"""
    depth = 1; i = start
    while i < len(s) and depth > 0:
        if s[i] == '{': depth += 1
        elif s[i] == '}': depth -= 1
        i += 1
    return i - 1


def _to_sub(text):
    return ''.join(SUB_MAP.get(c, c) for c in text)


def _to_sup(text):
    return ''.join(SUP_MAP.get(c, c) for c in text)


def _parse_latex(s):
    """LaTeX → 文本（递归解析 \\command, ^, _, {}）"""
    out = []; i = 0
    while i < len(s):
        ch = s[i]
        if ch == '\\':
            j = i + 1
            while j < len(s) and s[j].isalpha(): j += 1
            cmd = s[i+1:j]; i = j
            if cmd == 'frac':
                if i < len(s) and s[i] == ' ': i += 1
                num = ''
                if i < len(s) and s[i] == '{':
                    end = _find_matching_brace(s, i+1)
                    num = _parse_latex(s[i+1:end]); i = end + 1
                if i < len(s) and s[i] == '{':
                    end = _find_matching_brace(s, i+1)
                    den = _parse_latex(s[i+1:end]); i = end + 1
                else: den = ''
                out.append(f'({num})/({den})')
            elif cmd in TEXT_TAGS:
                if i < len(s) and s[i] == '{':
                    end = _find_matching_brace(s, i+1)
                    out.append(s[i+1:end]); i = end + 1
            elif cmd in GREEK:
                out.append(GREEK[cmd])
            elif cmd in SYMS:
                out.append(SYMS[cmd])
            elif cmd in ('max','min','sin','cos','log','ln'):
                if i < len(s) and s[i] == '_':
                    i += 1
                    if i < len(s) and s[i] == '{':
                        end = _find_matching_brace(s, i+1)
                        inner = _parse_latex(s[i+1:end]); i = end + 1
                        out.append(f'{cmd}_{inner}')
                    elif i < len(s):
                        out.append(f'{cmd}_'); i += 1
                else: out.append(cmd)
            else: out.append('\\' + cmd)
        elif ch == '^':
            i += 1
            if i < len(s) and s[i] == '{':
                end = _find_matching_brace(s, i+1)
                inner = _parse_latex(s[i+1:end]); i = end + 1
                out.append(_to_sup(inner))
            elif i < len(s): out.append(_to_sup(s[i])); i += 1
        elif ch == '_':
            i += 1
            if i < len(s) and s[i] == '{':
                end = _find_matching_brace(s, i+1)
                inner = _parse_latex(s[i+1:end]); i = end + 1
                out.append(_to_sub(inner))
            elif i < len(s): out.append(_to_sub(s[i])); i += 1
        elif ch == '{':
            end = _find_matching_brace(s, i+1)
            out.append(_parse_latex(s[i+1:end])); i = end + 1
        elif ch == ' ':
            if out and not out[-1].endswith(' '): out.append(' ')
            i += 1
        else: out.append(ch); i += 1
    return ''.join(out)


def latex_to_unicode(latex):
    """将 LaTeX 公式转换为 Unicode 文本"""
    return _parse_latex(latex.strip())
```

**步骤 2** — 在解析 Markdown 循环的 `$$...$$` 处理部分，替换为：

```python
    if line.startswith('$$'):
        if in_math:
            formula = '\n'.join(math_buf).strip()
            add_formula_paragraph(formula)
            math_buf = []; in_math = False
        else: in_math = True
        i += 1; continue
    if in_math: math_buf.append(line); i += 1; continue
```

**步骤 3** — 添加公式段落插入函数：

```python
def add_formula_paragraph(latex):
    """居中显示数学公式，Times New Roman 字体"""
    txt = latex_to_unicode(latex)
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    pf = p.paragraph_format
    pf.line_spacing = 1.5
    pf.space_before = Pt(6)
    pf.space_after = Pt(6)
    run = p.add_run(txt)
    set_run_font(run, name_cn='Times New Roman', name_en='Times New Roman', size=Pt(11), italic=True)
```

> 注意：`set_run_font` 函数中需要加一个 `italic` 参数：
> `def set_run_font(run, name_cn='宋体', name_en='宋体', size=Pt(10.5), bold=False, italic=False, color=RGBColor(0, 0, 0)):`

---

### 工作原理

| LaTeX 输入 | → Unicode 输出 |
|-----------|----------------|
| `\mathrm{SMA}(N)_t` | `SMA(N)ₜ` |
| `\frac{C_t + C_{t-1}}{N}` | `(Cₜ + Cₜ₋₁)/(N)` |
| `\alpha \cdot \beta` | `α · β` |
| `\min_{0 \leq s \leq t}` | `min_0 ≤ s ≤ t` |
| `\sigma_p` | `σₚ` |
| `R_{\mathrm{cumulative}}` | `Rcumulative` |

生成的 Word 文档中，所有公式都是**纯文本**（Times New Roman 斜体，11pt），不会有任何图片或 LaTeX 源码残留。
